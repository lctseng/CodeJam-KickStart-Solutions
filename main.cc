#include <algorithm>
#include <iostream>
using namespace std;

// static const auto _ = []() {
//     std::ios::sync_with_stdio( false );
//     std::cin.tie( nullptr );
//     return nullptr;
// }( );

int main() {
  int T;
  cin >> T;
  for (int t = 1; t <= T; t++) {
    // TODO: read input for each case and compute output
    cout << "Case #" << t << ": " << 123 << endl;
  }
  return 0;
}