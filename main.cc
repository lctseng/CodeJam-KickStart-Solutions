#include <algorithm>
#include <iostream>
using namespace std;

int main() {
  int T;
  cin >> T;
  for (int t = 1; t <= T; t++) {
    // TODO: read input for each case and compute output
    cout << "Case #" << t << ": " << 123 << endl;
  }
  return 0;
}